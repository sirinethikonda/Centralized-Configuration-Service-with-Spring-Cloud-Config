spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: "health,refresh"
